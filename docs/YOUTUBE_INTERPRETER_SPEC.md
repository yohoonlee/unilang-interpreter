# YouTube 통역 서비스 기술 명세서

## 1. 서비스 개요

YouTube 영상의 자막을 자동 추출하여 다국어로 번역하고, AI 음성으로 읽어주는 실시간 통역 서비스입니다.

### 1.1 핵심 기능

| 기능 | 설명 |
|------|------|
| 자막 자동 추출 | YouTube 영상에서 자막 자동 다운로드 |
| AI 문장 재정리 | 끊어진 자막을 자연스러운 문장으로 합침 |
| 다국어 번역 | 16개 이상 언어 지원 |
| AI 음성 재생 | Google Cloud TTS로 번역문 읽기 |
| 영상 동기화 | 자막과 영상 시간 동기화 |
| AI 요약 | 전체 내용 요약 생성 |

### 1.2 지원 언어

한국어, 영어, 일본어, 중국어, 스페인어, 독일어, 프랑스어, 이탈리아어, 포르투갈어, 러시아어, 아랍어, 힌디어, 태국어, 베트남어, 인도네시아어, 터키어

---

## 2. 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 브라우저                           │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐  │
│  │ YouTube URL │ -> │  메인 페이지  │ -> │    재생 페이지       │  │
│  │   입력      │    │  (page.tsx) │    │ (live/page.tsx)    │  │
│  └─────────────┘    └─────────────┘    └─────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          │                   │                    │
          v                   v                    v
┌─────────────────────────────────────────────────────────────────┐
│                      Next.js API Routes                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │/api/     │  │/api/     │  │/api/     │  │/api/cache/       │ │
│  │youtube/  │  │gemini/   │  │tts       │  │subtitle          │ │
│  │transcript│  │reorganize│  │          │  │                  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
          │                   │                    │
          v                   v                    v
┌─────────────────────────────────────────────────────────────────┐
│                       외부 서비스                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │
│  │ Railway  │  │ Google   │  │ Google   │  │    Supabase      │ │
│  │ Subtitle │  │ Gemini   │  │Cloud TTS │  │   (PostgreSQL)   │ │
│  │ Server   │  │   API    │  │   API    │  │                  │ │
│  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 처리 흐름

### 3.1 전체 워크플로우

```
[1] URL 입력 & 캐시 확인
         │
         ├── 캐시 있음 ──> [8] 바로 재생
         │
         v (캐시 없음)
[2] 자막 다운로드 (Railway Server)
         │
         v
[3] 원본 언어 감지 (Google Translation API)
         │
         v
[4] AI 문장 재정리 (Google Gemini)
         │
         v
[5] 배치 번역 (Google Cloud Translation)
         │
         v
[6] AI 요약 생성 (Google Gemini)
         │
         v
[7] 저장 (localStorage + Supabase)
         │
         v
[8] 재생 페이지 이동 & 동기화 시작
```

### 3.2 AI 문장 재정리 상세

**문제**: YouTube 자막은 문장이 끊어져 있음

```
입력:
[1] Hello everyone, today
[2] we are going to talk about
[3] artificial intelligence.
[4] It is very important
[5] for our future.
```

**해결**: Gemini AI로 문장 합치기

```
출력:
[
  { "merged_from": [1,2,3], "text": "Hello everyone, today we are going to talk about artificial intelligence." },
  { "merged_from": [4,5], "text": "It is very important for our future." }
]
```

---

## 4. TTS (Text-to-Speech) 시스템

### 4.1 Google Cloud TTS 음성 목록

| 언어 | 여성 음성 | 남성 음성 |
|------|----------|----------|
| 한국어 | Neural2-A, Neural2-B, Wavenet-A/B | Neural2-C, Wavenet-C/D |
| 영어 | Neural2-C/E/F/G/H | Neural2-A/D/I/J |
| 일본어 | Neural2-B | Neural2-C/D |
| 중국어 | Neural2-A/C | Neural2-B/D |

### 4.2 TTS 제어 함수

| 함수 | 용도 | 호출 시점 |
|------|------|----------|
| `playTTS(text, lang)` | TTS 재생 | 자막 변경 시 |
| `pauseTTS()` | 일시정지 (위치 유지) | 영상 일시정지 |
| `resumeTTS()` | 이어서 재생 | 영상 재생 재개 |
| `stopTTS()` | 완전 중지 | 시간 이동, 모드 전환 |

### 4.3 시간 이동 시 스마트 처리

```
시간 이동 감지 (자막 인덱스 2 이상 변경)
         │
         v
남은 시간 계산 = 다음 자막 시작시간 - 현재 영상시간
         │
         ├── 남은 시간 < 3초 ──> 현재 자막 건너뛰기
         │
         └── 남은 시간 >= 3초 ──> 1.5배속으로 재생
```

### 4.4 TTS 설정

| 설정 | 기본값 | 범위 | 저장 |
|------|--------|------|------|
| 속도 | 1.3x | 0.5 ~ 2.0 | localStorage |
| 음성 | 자동 (여성) | 언어별 목록 | localStorage |

---

## 5. API 명세

### 5.1 자막 추출 API

**POST** `/api/youtube/transcript`

```json
// Request
{
  "videoId": "dQw4w9WgXcQ",
  "lang": "en"
}

// Response
{
  "success": true,
  "utterances": [
    { "start": 0, "text": "Hello everyone" },
    { "start": 2500, "text": "Welcome to my channel" }
  ],
  "language": "en",
  "videoTitle": "Video Title",
  "duration": 215
}
```

### 5.2 AI 재정리 API

**POST** `/api/gemini/reorganize`

```json
// Request
{
  "utterances": [
    { "id": 1, "text": "Hello everyone, today" },
    { "id": 2, "text": "we will talk about AI." }
  ],
  "targetLanguage": "en"
}

// Response
{
  "success": true,
  "data": [
    { "merged_from": [1, 2], "text": "Hello everyone, today we will talk about AI." }
  ],
  "model": "gemini-2.0-flash"
}
```

### 5.3 TTS API

**POST** `/api/tts`

```json
// Request
{
  "text": "안녕하세요",
  "languageCode": "ko",
  "speed": 1.3,
  "voiceName": "ko-KR-Neural2-A"
}

// Response
{
  "success": true,
  "audioContent": "//NExAAQ...(base64)",
  "voice": "ko-KR-Neural2-A"
}
```

### 5.4 자막 캐시 API

**GET** `/api/cache/subtitle?videoId=xxx&lang=ko`

**POST** `/api/cache/subtitle`
```json
{
  "videoId": "xxx",
  "lang": "ko",
  "utterances": [...],
  "summary": "..."
}
```

---

## 6. 데이터베이스 스키마

### 6.1 video_subtitles_cache

영상별 자막/번역 캐시 저장

```sql
CREATE TABLE video_subtitles_cache (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  video_id TEXT NOT NULL,
  original_lang TEXT,
  original_utterances JSONB,    -- 원본 자막
  translations JSONB,           -- {"ko": [...], "en": [...]}
  summary JSONB,                -- {"ko": "...", "en": "..."}
  video_title TEXT,
  video_duration INTEGER,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(video_id)
);
```

### 6.2 user_video_history

사용자별 시청 기록

```sql
CREATE TABLE user_video_history (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  video_id TEXT NOT NULL,
  video_title TEXT,
  original_lang TEXT,
  target_lang TEXT,
  is_starred BOOLEAN DEFAULT FALSE,
  last_viewed_at TIMESTAMPTZ DEFAULT NOW(),
  created_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(user_id, video_id, target_lang)
);
```

---

## 7. 컴포넌트 구조

### 7.1 메인 페이지 (`youtube/page.tsx`)

| 컴포넌트/함수 | 역할 |
|--------------|------|
| `startIntegratedLiveMode()` | 전체 워크플로우 실행 |
| `translateBatchForWorkflow()` | 배치 번역 |
| `summarizeTextForWorkflow()` | 요약 생성 |
| `loadYoutubeHistory()` | 사용 기록 로드 |

### 7.2 재생 페이지 (`youtube/live/page.tsx`)

| 컴포넌트/함수 | 역할 |
|--------------|------|
| `startSyncTimer()` | 자막-영상 동기화 타이머 |
| `playTTS()` / `pauseTTS()` / `stopTTS()` | TTS 제어 |
| `seekToUtterance()` | 자막 클릭 시 시간 이동 |
| `toggleAudioMode()` | 화자음성 ↔ 번역음성 전환 |

---

## 8. UI 스타일 가이드

### 8.1 색상 팔레트

| 요소 | 색상 코드 | 용도 |
|------|----------|------|
| Primary | `#14B8A6` | 타이틀, 테이블 헤더 |
| Secondary | `#CCFBF1` | 컨트롤 패널, 호버 |
| Background | `#FFFFFF` | 페이지 배경 |
| Danger | `#DC2626` | 음성 토글 버튼 |
| Purple | `#9333EA` | AI 재정리 버튼 |

### 8.2 버튼 레이아웃

**창모드 하단**
```
[🔊 화자/번역] [↗ 전체화면] [📖 자막 크기] [🤖 AI 재정리]
   빨간색         빨간색        회색          보라색
```

**전체화면 상단**
```
[🔊 화자/번역] [속도 -/+] [⛶ 창모드]
   빨간색                    회색
```

---

## 9. 환경 설정

### 9.1 Vercel 환경변수

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Google Cloud (Translation, TTS, Gemini)
GOOGLE_API_KEY=AIzaSy...
NEXT_PUBLIC_GOOGLE_API_KEY=AIzaSy...
```

### 9.2 Google Cloud API 활성화 필수

1. **Cloud Translation API** - 번역
2. **Cloud Text-to-Speech API** - 음성 합성
3. **Generative Language API** - AI 재정리/요약

### 9.3 Railway 환경변수 (자막 서버)

```env
PORT=8080
WEBSHARE_PROXY_URL=http://user:pass@proxy.webshare.io:80
```

---

## 10. 파일 구조

```
frontend/app/
├── api/
│   ├── tts/route.ts                 # Google Cloud TTS
│   ├── translate/route.ts           # Google Translation
│   ├── gemini/
│   │   ├── reorganize/route.ts      # AI 문장 재정리
│   │   └── summarize/route.ts       # AI 요약
│   ├── cache/subtitle/route.ts      # 자막 캐시
│   └── youtube/transcript/route.ts  # 자막 추출
│
└── service/translate/youtube/
    ├── page.tsx                     # 메인 페이지 (URL 입력, 처리)
    └── live/
        └── page.tsx                 # 재생 페이지 (동기화, TTS)

subtitle-server/                     # Railway 배포
├── main.py                          # FastAPI 서버
├── requirements.txt
└── Procfile
```

---

## 11. 알려진 제한사항

| 제한 | 설명 | 대안 |
|------|------|------|
| 자막 없는 영상 | 자막이 없으면 처리 불가 | 실시간 STT 모드 전환 |
| 긴 영상 (2시간+) | 처리 시간 증가 | 백그라운드 처리 |
| 음성 변경 | 다음 자막부터 적용 | 현재 재생 중인 자막은 유지 |
| 동시 재생 | 탭당 1개 영상만 | 새 탭에서 추가 재생 |

---

*문서 버전: 1.0*  
*최종 수정: 2024-12*

