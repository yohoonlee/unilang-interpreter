# YouTube 실시간 통역 기능 기술 문서

> 최종 업데이트: 2024년 12월 3일

## 📋 목차

1. [기능 개요](#기능-개요)
2. [시스템 아키텍처](#시스템-아키텍처)
3. [핵심 기술 스택](#핵심-기술-스택)
4. [Deepgram 연동](#deepgram-연동)
5. [기능 상세](#기능-상세)
6. [데이터 저장 구조](#데이터-저장-구조)
7. [API 엔드포인트](#api-엔드포인트)
8. [UI/UX 구성](#uiux-구성)
9. [참조 사이트](#참조-사이트)

---

## 기능 개요

YouTube 영상의 음성을 실시간으로 인식하고 번역하는 기능입니다.

### 주요 기능

| 기능 | 설명 |
|------|------|
| **실시간 음성 인식** | Deepgram Nova-2 모델을 사용한 고정밀 음성 인식 |
| **실시간 번역** | Google Translate API를 통한 다국어 번역 |
| **전체화면 모드** | YouTube 영상 + 하단 자막 오버레이 |
| **크게보기/작게보기** | 자막 표시 크기 토글 |
| **자동 저장** | 공유 중지 시 로컬 + DB 자동 저장 |
| **다시보기** | 저장된 통역 내용 불러오기 |
| **AI 재정리** | Gemini API를 통한 문장 재구성 |
| **요약 생성** | Gemini API를 통한 내용 요약 |

---

## 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        브라우저 (Chrome)                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐  │
│  │  YouTube    │    │   탭 오디오  │    │  음성 인식 결과     │  │
│  │  iframe     │───▶│   캡처      │───▶│  + 번역 표시        │  │
│  └─────────────┘    └─────────────┘    └─────────────────────┘  │
│                            │                                     │
│                            ▼                                     │
│                   ┌─────────────────┐                           │
│                   │  WebSocket      │                           │
│                   │  (PCM 16-bit)   │                           │
│                   └────────┬────────┘                           │
└────────────────────────────┼────────────────────────────────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │   Deepgram      │
                    │   Nova-2 API    │
                    └────────┬────────┘
                             │
                             ▼
                    ┌─────────────────┐
                    │  Google         │
                    │  Translate API  │
                    └─────────────────┘
```

---

## 핵심 기술 스택

### Frontend
- **Next.js 16.0.3** (App Router)
- **React 19**
- **TypeScript**
- **Tailwind CSS**
- **Supabase Client** (인증 + DB)

### APIs
- **Deepgram** - 실시간 음성 인식 (STT)
- **Google Translate** - 다국어 번역
- **Google Gemini** - AI 재정리/요약
- **YouTube oEmbed** - 영상 정보 조회

### 오디오 처리
- **Web Audio API** - 오디오 스트림 처리
- **getDisplayMedia** - 시스템 오디오 캡처
- **ScriptProcessorNode** - PCM 변환

---

## Deepgram 연동

### 1. API 키 설정

```bash
# .env.local
DEEPGRAM_API_KEY=your_deepgram_api_key_here
```

### 2. API 키 발급 방법

1. [Deepgram Console](https://console.deepgram.com/) 접속
2. 회원가입 또는 로그인
3. **API Keys** 메뉴에서 새 키 생성
4. 생성된 키를 `.env.local`에 추가

### 3. 토큰 API 엔드포인트

**경로:** `/api/deepgram/token`

```typescript
// frontend/app/api/deepgram/token/route.ts
export async function POST(request: NextRequest) {
  const apiKey = process.env.DEEPGRAM_API_KEY
  
  if (!apiKey) {
    return NextResponse.json({ 
      error: "Deepgram API 키가 설정되지 않았습니다" 
    }, { status: 500 })
  }

  return NextResponse.json({ apiKey })
}
```

### 4. WebSocket 연결

```typescript
const ws = new WebSocket(
  `wss://api.deepgram.com/v1/listen?` +
  `encoding=linear16&` +
  `sample_rate=16000&` +
  `channels=1&` +
  `model=nova-2&` +
  `language=${language}&` +
  `punctuate=true&` +
  `interim_results=true`,
  ["token", apiKey]
)
```

### 5. 오디오 데이터 전송

```typescript
// Float32 to Int16 (PCM) 변환
const convertFloat32ToInt16 = (float32Array: Float32Array): Int16Array => {
  const int16Array = new Int16Array(float32Array.length)
  for (let i = 0; i < float32Array.length; i++) {
    const s = Math.max(-1, Math.min(1, float32Array[i]))
    int16Array[i] = s < 0 ? s * 0x8000 : s * 0x7FFF
  }
  return int16Array
}

// 오디오 처리
processor.onaudioprocess = (e) => {
  if (ws.readyState === WebSocket.OPEN) {
    const inputData = e.inputBuffer.getChannelData(0)
    const pcmData = convertFloat32ToInt16(inputData)
    ws.send(pcmData.buffer)
  }
}
```

### 6. Deepgram 응답 처리

```typescript
ws.onmessage = async (event) => {
  const data = JSON.parse(event.data)
  
  if (data.type === "Results" && data.channel?.alternatives?.[0]) {
    const transcript = data.channel.alternatives[0].transcript
    
    if (data.is_final && transcript?.trim()) {
      // 최종 인식 결과 처리
      await processUtterance(transcript.trim())
    } else if (transcript) {
      // 중간 인식 결과 (interim)
      setCurrentTranscript(transcript)
    }
  }
}
```

### 7. 지원 언어

| 언어 코드 | 언어명 | Deepgram 코드 |
|-----------|--------|---------------|
| ko | 한국어 | ko |
| en | 영어 | en |
| ja | 일본어 | ja |
| zh | 중국어 | zh |
| es | 스페인어 | es |
| fr | 프랑스어 | fr |
| de | 독일어 | de |

---

## 기능 상세

### 1. 시스템 오디오 캡처

```typescript
const stream = await navigator.mediaDevices.getDisplayMedia({
  video: true,
  audio: {
    echoCancellation: false,
    noiseSuppression: false,
    autoGainControl: false,
  },
  // Chrome 전용 옵션
  preferCurrentTab: true,
  selfBrowserSurface: "include",
})
```

**주의사항:**
- Chrome 브라우저 필수
- "탭 오디오도 공유" 체크 필요
- 하울링 방지를 위해 GainNode 사용 (`gain.value = 0`)

### 2. 전체화면 모드

```typescript
// Fullscreen API 사용
const enterFullscreen = async () => {
  await fullscreenContainerRef.current?.requestFullscreen()
  setIsFullscreen(true)
}

const exitFullscreen = async () => {
  await document.exitFullscreen()
  setIsFullscreen(false)
}
```

**전체화면 UI 구성:**
- 상단: UniLang 로고 + 통역 상태 + YouTube 제목 + 창모드 버튼
- 중앙: YouTube 영상 (100vh)
- 하단: 자막 오버레이 (원어 + 번역어)

### 3. 자동 저장

**저장 시점:**
- 공유 중지 시
- 10개 문장마다
- AI 재정리/요약 후
- 수동 저장 버튼 클릭 시

**저장 위치:**
- 로컬 스토리지: `unilang_youtube_{videoId}_{sourceLang}_{targetLang}`
- Supabase DB: `translation_sessions` + `utterances` + `translations` 테이블

### 4. 다시보기 선택

저장된 데이터가 있으면 선택 모달 표시:
- **"저장된 내용 보기"** - 기존 데이터 로드
- **"새로 통역하기"** - 새로 시작

---

## 데이터 저장 구조

### 로컬 스토리지

```typescript
interface SavedSession {
  videoId: string
  sourceLang: string
  targetLang: string
  utterances: Utterance[]
  savedAt: string
  summary?: string
}

interface Utterance {
  id: string
  original: string
  translated: string
  timestamp: Date
  startTime: number
}
```

### Supabase DB

**translation_sessions 테이블:**
```sql
id: uuid
user_id: uuid
title: text -- "{YouTube제목} ({원어} → {번역어})"
session_type: text -- "youtube"
source_language: text
target_languages: text[]
youtube_video_id: text
youtube_title: text
status: text
total_utterances: integer
started_at: timestamp
ended_at: timestamp
```

**utterances 테이블:**
```sql
id: uuid
session_id: uuid (FK)
original_text: text
original_language: text
created_at: timestamp
```

**translations 테이블:**
```sql
id: uuid
utterance_id: uuid (FK)
translated_text: text
target_language: text
```

---

## API 엔드포인트

### Deepgram 토큰
- **POST** `/api/deepgram/token`
- 응답: `{ apiKey: string }`

### YouTube 정보
- **GET** `/api/youtube/info?v={videoId}`
- 응답: `{ success: true, title: string, author: string, thumbnail: string }`

### AI 재정리
- **POST** `/api/gemini/reorganize`
- 요청: `{ utterances: [...], targetLanguage: string }`
- 응답: `{ success: true, data: [...] }`

### 요약 생성
- **POST** `/api/gemini/summarize`
- 요청: `{ text: string, language: string }`
- 응답: `{ success: true, summary: string }`

---

## UI/UX 구성

### 일반 모드

```
┌─────────────────────────────────────────────────────────────┐
│                    YouTube 영상 (55vh)                       │
├─────────────────────────────────────────────────────────────┤
│ 📺 {YouTube 제목}                                            │
├─────────────────────────────────────────────────────────────┤
│ 🌐UniLang 통역중 │ 언어 │ 전체화면 │ 크게보기 │ 시작/중지    │
├─────────────────────────────────────────────────────────────┤
│ 📌 사용법 안내                                               │
├─────────────────────────────────────────────────────────────┤
│ 자막 히스토리 (30vh, 스크롤)                                 │
│ ┌──────────────────────────────────────────────────────┐    │
│ │ #1 원어 텍스트                                        │    │
│ │    번역된 텍스트                                      │    │
│ └──────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────┤
│ 총 N개 문장 │ [AI재정리] [요약] [저장]                       │
└─────────────────────────────────────────────────────────────┘
```

### 전체화면 모드

```
┌─────────────────────────────────────────────────────────────┐
│ 🌐UniLang ●통역중  📺제목...            [⛶ 창 모드]         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                                                             │
│                    YouTube 영상 (100vh)                     │
│                                                             │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  ░░░░░░░░░░░░░░░ 그라데이션 배경 ░░░░░░░░░░░░░░░░░░░░░░░░   │
│           원어 텍스트 (흰색, 큰 글씨)                        │
│           번역 텍스트 (녹색, 굵은 글씨)          [창모드전환] │
└─────────────────────────────────────────────────────────────┘
```

### 크게보기 모드
- 자막 영역에 최근 2개 문장만 큰 글씨로 표시
- 원어: `text-xl`
- 번역어: `text-2xl font-bold`

---

## 참조 사이트

### Deepgram
- **공식 문서:** https://developers.deepgram.com/docs
- **Console (API 키 발급):** https://console.deepgram.com/
- **WebSocket 스트리밍:** https://developers.deepgram.com/docs/getting-started-with-live-streaming-audio
- **Nova-2 모델:** https://developers.deepgram.com/docs/models-overview#nova-2
- **지원 언어:** https://developers.deepgram.com/docs/languages-overview

### Google APIs
- **Cloud Translation:** https://cloud.google.com/translate/docs
- **Gemini API:** https://ai.google.dev/docs

### Web APIs
- **Web Audio API:** https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API
- **getDisplayMedia:** https://developer.mozilla.org/en-US/docs/Web/API/MediaDevices/getDisplayMedia
- **Fullscreen API:** https://developer.mozilla.org/en-US/docs/Web/API/Fullscreen_API

### YouTube
- **oEmbed API:** https://oembed.com/
- **YouTube Embed:** https://developers.google.com/youtube/player_parameters

---

## 변경 이력

### 2024-12-03

#### 추가된 기능
1. **YouTube 실시간 통역 새 창 모드**
   - 90% 화면 크기로 새 창 열기
   - `autostart=true` 파라미터로 자동 시작

2. **전체화면 모드 + 하단 자막 오버레이**
   - Fullscreen API 사용
   - 최신 완성된 자막 1줄 표시 (원어 + 번역어)
   - 상단/하단에 창모드 전환 버튼

3. **크게보기/작게보기 토글**
   - 최근 2개 문장만 큰 글씨로 표시
   - 공간 절약 모드

4. **YouTube 제목 표시**
   - `/api/youtube/info` API 추가
   - 컨트롤 바 상단에 제목 표시

5. **자동 저장 기능**
   - 공유 중지 시 자동 저장 (로컬 + DB)
   - 10개 문장마다 자동 저장

6. **다시보기 선택 모달**
   - 저장된 데이터 있을 시 선택 모달 표시
   - "저장된 내용 보기" / "새로 통역하기"

7. **YouTube 통역 기록 목록 UI**
   - 메인 페이지에 목록 아이콘 버튼
   - 썸네일 + 제목 + 날짜 + 다시보기 버튼

#### 수정된 사항
- ScriptProcessor → GainNode 연결 (하울링 방지)
- DB 저장 오류 수정 (로컬 스토리지 우선)
- 빌드 에러 수정 (JSX 구문 오류)

---

## 환경 변수

```bash
# .env.local

# Deepgram
DEEPGRAM_API_KEY=your_deepgram_api_key

# Google
NEXT_PUBLIC_GOOGLE_API_KEY=your_google_api_key

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

---

## 트러블슈팅

### 1. 자막이 표시되지 않음
- **원인:** ScriptProcessor가 destination에 연결되지 않음
- **해결:** GainNode(gain=0)를 통해 destination에 연결

### 2. 하울링 발생
- **원인:** 오디오 출력이 다시 입력으로 들어감
- **해결:** GainNode의 gain.value를 0으로 설정

### 3. 화면 공유 취소됨
- **원인:** 사용자가 취소 또는 "탭 오디오 공유" 미체크
- **해결:** 안내 메시지 표시 + 오류 처리

### 4. Deepgram 연결 실패
- **원인:** API 키 미설정 또는 만료
- **해결:** `.env.local`에 유효한 API 키 설정

### 5. DB 저장 실패
- **원인:** 로그인 안 됨 또는 테이블 미존재
- **해결:** 로컬 스토리지에 우선 저장, 로그인 시 DB 저장

---

## 라이선스

MIT License

